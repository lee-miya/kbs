---
description: KBS 知识库维护与周更约定（编辑文档时参考）
globs: "**/*.md"
alwaysApply: false
---

# KBS 文档维护

- 只写公开技术知识；禁止真实目标、IP、域名、凭证、内部组织名。
- 禁止完整可打穿生产的 exploit 载荷；写原理、条件、识别特征、建议动作。
- 权威版本是各分册；审计周更**直接写入**分册「时效条目」，不建增量池。
- 红队周报在 `12_每周情报/`，有价值条目回链 `09`/`10` 等。
- 周更检索须**大范围**：遵守 `维护/周更重点关注源.md`（专家/博客/厂商/论坛/CTF/免杀/钓鱼/边界/**同类 skill 库**），不得只扫 KEV/聚合站。
- **AI 代码审计案例**优先精析：管线分工、提示词结构、验证闭环、可迁移纪律 → 综合分册「五」+ 方法论「十一」。
- **CTF 奇技淫巧**每周硬性提炼：可迁移手法（绕过/原语/语言 quirk）→ 周报 + 分册时效或综合「CTF 拾遗」；禁止题解流水账。
- **免杀/EDR**每周硬性提炼：检测层 + 手法 + 自测 → 周报 3.6 + `06-02` 时效；禁止完整生产级 loader/shellcode。
- **钓鱼/初始访问**每周硬性提炼：路径类型 + 手法 + 自测 → 周报 3.7 + `03-01` 时效；禁止完整 PhaaS/生产诱饵包。
- **边界设备**每周硬性提炼：产品族 + 指纹 + CVE/方向 + 建议动作 → 周报 3.8 + `09-03` 时效；禁止完整生产级 exploit。
- **同类 AI 攻防 skill 库**每周硬性对照：新攻击面/工作流/缺口/审查结论 → 周报 3.9；禁止第三方 SKILL.md 入库。
- 体例：`维护/文档体例约定.md`；流程：`维护/每周更新SOP.md`。
- 新增模式够高频再进 `INDEX.md` 速查表。
- 单一来源未经厂商证实标注「待核实」。
- 双库分工：审计=源码模式；红队=场景命令；交叉引用用相对路径。
- Agent 作战入口：红队 `INDEX.md`、审计 `INDEX.md`（本地若有 `AGENTS.md` 先读）；改 `.agents/skills/` 须跑 `维护/review-kbs-skills.ps1`。
