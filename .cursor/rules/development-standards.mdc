---
description: 开发规范：交付流程、代码分层、测试与环境配置
globs: src/**,docs/**,README.md,scripts/**,**/*test*,**/*Test*
alwaysApply: false
---

# 开发规范

## 分阶段交付

- 大功能按 Batch/Phase 拆分，单次变更范围可控、可独立验收
- 维护「对外功能 → 内部实现 → 状态」映射文档，与代码同步更新
- 每阶段结束：映射 Status、测试矩阵、Usage 示例一并更新

### 兼容与演进

- 若项目为迁移/重写：对外 CLI、API、参数名保持与上游或契约一致
- 内部模块命名与结构可按目标语言/框架自由设计，不与对外名称强绑定

### 阶段收尾 Checklist

- [ ] README 入口清晰（构建、运行、文档索引）
- [ ] 全功能 Usage / 操作示例
- [ ] 映射表或架构说明与实现一致
- [ ] 集成测试 example 配置可复现
- [ ] 经验总结写入 docs（通用模式与项目特例分开写）

## 代码分层与复用

### 结构

- **入口层**（CLI 子命令、HTTP handler、RPC）：解析参数、调用服务、格式化输出、映射退出码
- **领域/服务层**：业务逻辑、外部系统交互（DB、LDAP、文件等）
- **共享层**：连接、鉴权、过滤、序列化、错误码等横切关注点

### 禁止

- 在每个入口重复实现相同的参数解析、连接建立、响应格式化
- 入口层直接堆砌复杂业务 SQL/HTTP 调用（应下沉到服务）

### 新增功能时

1. 判断是否已有可复用的服务或共享模块
2. 新入口只做「注册 + 委托 + 输出」
3. 新入口至少 1 个单元测试（参数、退出码、输出结构）

### 命名

- 对外名称遵循项目契约（如上游工具原名）
- 内部类型/模块名遵循目标语言惯例，不必与对外名称相同

## 测试与环境配置

### 测试三层

| 层级 | 目标 | 做法 |
|------|------|------|
| 单元 | 参数解析、格式化、退出码、纯逻辑 | Fake/Mock 依赖，快速、无外部服务 |
| 集成 | 真实环境端到端 | 一键脚本 + 可配置目标 |
| 对照/契约 | 与原版或基准行为一致 | 语义等价，不要求字节级相同 |

### 配置分离

- **入仓**：`*.example.*`、模板、虚构主机名/IP/凭证的文档示例
- **不入仓**：真实 env 文件、本地实验配置、密钥、`.cursor/`、本地参考目录
- 集成测试脚本从 gitignored 本地配置读取目标，example 文件说明每个变量含义

### 新增测试时

- 单元测试优先覆盖边界（缺参、错误凭据、空结果）
- 集成用例编号与测试计划文档对应，便于追溯
- 文档与示例中的主机名、路径、密码必须脱敏（见 `workspace-conventions.mdc`）
